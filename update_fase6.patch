From a0ba1d1a0ba5fee3c7f9e8f47dd1e13580ae302a Mon Sep 17 00:00:00 2001
From: Thiago Corobinski Carmona <carmona.c.thiago@gmail.com>
Date: Thu, 17 Sep 2026 11:25:16 -0300
Subject: [PATCH] feat: saldo unificado no simulador, operacoes de cambio em
 moeda estrangeira, fallback categoria despesa compartilhada e vinculo chave
 pix com conta

---
 .../0007_vincular_chave_pix_conta.sql         |   4 +
 .../despesas_compartilhadas.service.ts        |  13 +-
 backend/src/modules/pix/pix.schema.ts         |   5 +-
 backend/src/modules/pix/pix.service.ts        |  37 +-
 .../simuladores/simulador_gastos.schema.ts    |   2 +-
 .../simuladores/simulador_gastos.service.ts   |  11 +-
 frontend/src/pages/InvestimentosPage.tsx      | 421 +++++++++++++++---
 frontend/src/pages/PixPage.tsx                |  46 +-
 frontend/src/pages/SimuladorGastosPage.tsx    |   9 +-
 frontend/src/pages/SocialPage.tsx             |   2 +-
 frontend/src/types/index.ts                   |   2 +
 11 files changed, 456 insertions(+), 96 deletions(-)
 create mode 100644 backend/src/db/migrations/0007_vincular_chave_pix_conta.sql

diff --git a/backend/src/db/migrations/0007_vincular_chave_pix_conta.sql b/backend/src/db/migrations/0007_vincular_chave_pix_conta.sql
new file mode 100644
index 0000000..f88c1eb
--- /dev/null
+++ b/backend/src/db/migrations/0007_vincular_chave_pix_conta.sql
@@ -0,0 +1,4 @@
+-- Migração 0007: Vincular Chave PIX a uma Conta Bancária
+
+ALTER TABLE chave_pix ADD COLUMN IF NOT EXISTS conta_id UUID REFERENCES conta(id) ON DELETE SET NULL;
+CREATE INDEX IF NOT EXISTS idx_chave_pix_conta ON chave_pix(conta_id);
diff --git a/backend/src/modules/despesas_compartilhadas/despesas_compartilhadas.service.ts b/backend/src/modules/despesas_compartilhadas/despesas_compartilhadas.service.ts
index 298cefc..efde595 100644
--- a/backend/src/modules/despesas_compartilhadas/despesas_compartilhadas.service.ts
+++ b/backend/src/modules/despesas_compartilhadas/despesas_compartilhadas.service.ts
@@ -48,6 +48,17 @@ export class DespesasCompartilhadasService {
         const cRes = await client.query('SELECT moeda_id FROM conta WHERE id = $1', [dados.conta_origem_id]);
         if (cRes.rows.length > 0) {
           const moedaId = cRes.rows[0].moeda_id;
+          let categoriaId = dados.categoria_id;
+          if (!categoriaId) {
+            const catRes = await client.query("SELECT id FROM categoria WHERE tipo = 'despesa' ORDER BY criado_em ASC LIMIT 1");
+            if (catRes.rows.length > 0) {
+              categoriaId = catRes.rows[0].id;
+            } else {
+              const novaCat = await client.query("INSERT INTO categoria (nome, tipo, icone, cor) VALUES ('Outros Gastos', 'despesa', 'MoreHorizontal', '#6B7280') RETURNING id");
+              categoriaId = novaCat.rows[0].id;
+            }
+          }
+
           await client.query(`
             INSERT INTO lancamento (
               tipo, valor, moeda_id, data_compra, forma_pagamento, 
@@ -59,7 +70,7 @@ export class DespesasCompartilhadasService {
             moedaId,
             dados.data || null,
             dados.conta_origem_id,
-            dados.categoria_id || null,
+            categoriaId,
             `Despesa compartilhada: ${dados.descricao}`
           ]);
 
diff --git a/backend/src/modules/pix/pix.schema.ts b/backend/src/modules/pix/pix.schema.ts
index f246d6d..ac3ba2b 100644
--- a/backend/src/modules/pix/pix.schema.ts
+++ b/backend/src/modules/pix/pix.schema.ts
@@ -5,7 +5,8 @@ export const CriarChavePixSchema = z.object({
   valor_chave: z.string().min(3, 'Chave PIX inválida'),
   nome_recebedor: z.string().min(2, 'Nome do recebedor deve ter pelo menos 2 caracteres'),
   cidade_recebedor: z.string().min(2, 'Cidade do recebedor deve ter pelo menos 2 caracteres'),
-  apelido: z.string().optional()
+  apelido: z.string().optional(),
+  conta_id: z.preprocess(val => (val === '' || val === null ? undefined : val), z.string().uuid().optional())
 });
 
 export const CriarCobrancaPixSchema = z.object({
@@ -18,7 +19,7 @@ export const CriarCobrancaPixSchema = z.object({
 });
 
 export const ConfirmarCobrancaPixSchema = z.object({
-  conta_destino_id: z.string().uuid('Selecione a conta bancária para crédito')
+  conta_destino_id: z.preprocess(val => (val === '' || val === null ? undefined : val), z.string().uuid().optional())
 });
 
 export type CriarChavePixInput = z.infer<typeof CriarChavePixSchema>;
diff --git a/backend/src/modules/pix/pix.service.ts b/backend/src/modules/pix/pix.service.ts
index 1ce47b4..dc1d49e 100644
--- a/backend/src/modules/pix/pix.service.ts
+++ b/backend/src/modules/pix/pix.service.ts
@@ -10,6 +10,8 @@ export interface ChavePix {
   nome_recebedor: string;
   cidade_recebedor: string;
   apelido?: string;
+  conta_id?: string;
+  conta_nome?: string;
   ativo: boolean;
   criado_em: string;
 }
@@ -34,16 +36,24 @@ export interface CobrancaPixItem {
 
 export class PixService {
   async listarChaves(): Promise<ChavePix[]> {
-    const res = await query<ChavePix>('SELECT * FROM chave_pix WHERE ativo = TRUE ORDER BY criado_em ASC');
+    const res = await query<ChavePix>(`
+      SELECT 
+        cp.*,
+        c.apelido AS conta_nome
+      FROM chave_pix cp
+      LEFT JOIN conta c ON c.id = cp.conta_id
+      WHERE cp.ativo = TRUE 
+      ORDER BY cp.criado_em ASC
+    `);
     return res.rows;
   }
 
   async criarChave(dados: CriarChavePixInput): Promise<ChavePix> {
     const res = await query<ChavePix>(`
-      INSERT INTO chave_pix (tipo, valor_chave, nome_recebedor, cidade_recebedor, apelido)
-      VALUES ($1, $2, $3, $4, $5)
+      INSERT INTO chave_pix (tipo, valor_chave, nome_recebedor, cidade_recebedor, apelido, conta_id)
+      VALUES ($1, $2, $3, $4, $5, $6)
       RETURNING *
-    `, [dados.tipo, dados.valor_chave.trim(), dados.nome_recebedor.trim(), dados.cidade_recebedor.trim(), dados.apelido || null]);
+    `, [dados.tipo, dados.valor_chave.trim(), dados.nome_recebedor.trim(), dados.cidade_recebedor.trim(), dados.apelido || null, dados.conta_id || null]);
     return res.rows[0];
   }
 
@@ -133,6 +143,21 @@ export class PixService {
         throw new Error('Esta cobrança já foi confirmada anteriormente');
       }
 
+      // Resolve conta de destino (da chave vinculada ou padrão)
+      let contaDestinoId = dados.conta_destino_id;
+      if (!contaDestinoId) {
+        const cpRes = await client.query('SELECT conta_id FROM chave_pix WHERE id = $1', [cob.chave_pix_id]);
+        if (cpRes.rows.length > 0 && cpRes.rows[0].conta_id) {
+          contaDestinoId = cpRes.rows[0].conta_id;
+        } else {
+          const cFirst = await client.query('SELECT id FROM conta WHERE ativo = TRUE ORDER BY criado_em ASC LIMIT 1');
+          if (cFirst.rows.length > 0) contaDestinoId = cFirst.rows[0].id;
+        }
+      }
+      if (!contaDestinoId) {
+        throw new Error('Nenhuma conta bancária encontrada para receber o PIX');
+      }
+
       // 1. Marca cobrança como recebida
       await client.query(`
         UPDATE cobranca_pix 
@@ -144,12 +169,12 @@ export class PixService {
       if (cob.divida_id) {
         await dividasService.darBaixa(cob.divida_id, {
           valor: parseFloat(cob.valor),
-          conta_destino_id: dados.conta_destino_id,
+          conta_destino_id: contaDestinoId,
           forma_pagamento: 'pix'
         });
       } else {
         // Se cobrança avulsa, gera receita na conta destino
-        const contaRes = await client.query('SELECT moeda_id FROM conta WHERE id = $1', [dados.conta_destino_id]);
+        const contaRes = await client.query('SELECT moeda_id FROM conta WHERE id = $1', [contaDestinoId]);
         if (contaRes.rows.length > 0) {
           const moedaId = contaRes.rows[0].moeda_id;
           let catRes = await client.query("SELECT id FROM categoria WHERE nome = 'Outras Receitas' LIMIT 1");
diff --git a/backend/src/modules/simuladores/simulador_gastos.schema.ts b/backend/src/modules/simuladores/simulador_gastos.schema.ts
index e4015a4..c65442c 100644
--- a/backend/src/modules/simuladores/simulador_gastos.schema.ts
+++ b/backend/src/modules/simuladores/simulador_gastos.schema.ts
@@ -6,7 +6,7 @@ export const SimularGastoSchema = z.object({
   moeda_codigo: z.preprocess(val => (val === '' || val === null || val === undefined ? 'BRL' : val), z.string().default('BRL')),
   cotacao_personalizada: z.preprocess(val => (val === '' || val === null ? undefined : Number(val)), z.number().positive().optional()),
   forma_pagamento: z.enum(['a_vista', 'cartao_parcelado']),
-  conta_id: z.preprocess(val => (val === '' || val === null ? undefined : val), z.string().uuid().optional()),
+  conta_id: z.preprocess(val => (val === '' || val === null ? undefined : val), z.string().optional()),
   cartao_id: z.preprocess(val => (val === '' || val === null ? undefined : val), z.string().uuid().optional()),
   categoria_id: z.preprocess(val => (val === '' || val === null ? undefined : val), z.string().uuid().optional()),
   num_parcelas: z.preprocess(val => (val === '' || val === null || val === undefined ? 1 : Number(val)), z.number().int().min(1).max(36).default(1)),
diff --git a/backend/src/modules/simuladores/simulador_gastos.service.ts b/backend/src/modules/simuladores/simulador_gastos.service.ts
index 8eb39d1..2ddd3ae 100644
--- a/backend/src/modules/simuladores/simulador_gastos.service.ts
+++ b/backend/src/modules/simuladores/simulador_gastos.service.ts
@@ -72,18 +72,17 @@ export class SimuladorGastosService {
       let contaNome = 'Conta Corrente';
       let saldoAtual = 0;
 
-      if (dados.conta_id) {
+      if (dados.conta_id && dados.conta_id !== 'unificado') {
         const cRes = await query('SELECT apelido, saldo_atual FROM conta WHERE id = $1', [dados.conta_id]);
         if (cRes.rows.length > 0) {
           contaNome = cRes.rows[0].apelido;
           saldoAtual = parseFloat(cRes.rows[0].saldo_atual);
         }
       } else {
-        const cRes = await query('SELECT apelido, saldo_atual FROM conta WHERE ativo = TRUE ORDER BY criado_em ASC LIMIT 1');
-        if (cRes.rows.length > 0) {
-          contaNome = cRes.rows[0].apelido;
-          saldoAtual = parseFloat(cRes.rows[0].saldo_atual);
-        }
+        // Saldo Consolidado Unificado (Todas as contas ativas)
+        const sRes = await query('SELECT COALESCE(SUM(saldo_atual), 0) AS saldo_total FROM conta WHERE ativo = TRUE');
+        saldoAtual = parseFloat(sRes.rows[0]?.saldo_total || '0');
+        contaNome = 'Saldo Consolidado (Todas as Contas)';
       }
 
       return {
diff --git a/frontend/src/pages/InvestimentosPage.tsx b/frontend/src/pages/InvestimentosPage.tsx
index 446eefe..3b1ffa2 100644
--- a/frontend/src/pages/InvestimentosPage.tsx
+++ b/frontend/src/pages/InvestimentosPage.tsx
@@ -79,8 +79,21 @@ export const InvestimentosPage: React.FC = () => {
     carregarDados();
   }, []);
 
+  const [saldoInicialCambio, setSaldoInicialCambio] = useState({
+    habilitado: false,
+    quantidade: '',
+    cotacao: '',
+    data: new Date().toISOString().split('T')[0]
+  });
+
   const handleAbrirNovoAtivo = () => {
     setEditingAtivoId(null);
+    setSaldoInicialCambio({
+      habilitado: false,
+      quantidade: '',
+      cotacao: '',
+      data: new Date().toISOString().split('T')[0]
+    });
     setFormAtivo({
       tipo: 'renda_fixa',
       nome: '',
@@ -96,6 +109,12 @@ export const InvestimentosPage: React.FC = () => {
 
   const handleEditarAtivo = (ativo: PosicaoAtivo) => {
     setEditingAtivoId(ativo.id);
+    setSaldoInicialCambio({
+      habilitado: false,
+      quantidade: '',
+      cotacao: '',
+      data: new Date().toISOString().split('T')[0]
+    });
     const m = moedas.find(item => item.codigo === ativo.moeda_codigo);
     setFormAtivo({
       tipo: ativo.tipo,
@@ -127,7 +146,25 @@ export const InvestimentosPage: React.FC = () => {
       if (editingAtivoId) {
         await api.updateInvestimento(editingAtivoId, payload);
       } else {
-        await api.createInvestimento(payload);
+        const novo: any = await api.createInvestimento(payload);
+        const novoId = novo?.id || novo?.data?.id;
+
+        // Se cadastrou saldo inicial de moeda estrangeira, cria movimentação de aporte automaticamente
+        if (formAtivo.tipo === 'moeda_estrangeira' && saldoInicialCambio.habilitado && novoId) {
+          const qtd = parseFloat(saldoInicialCambio.quantidade);
+          const cot = parseFloat(saldoInicialCambio.cotacao);
+          if (qtd > 0 && cot > 0) {
+            await api.createMovimentacaoInvestimento({
+              investimento_id: novoId,
+              tipo: 'aporte',
+              valor: Math.round(qtd * cot * 100) / 100,
+              quantidade: qtd,
+              cotacao_praticada: cot,
+              data: saldoInicialCambio.data || new Date().toISOString().split('T')[0],
+              observacao: 'Saldo inicial de câmbio'
+            });
+          }
+        }
       }
       setShowModalNovoAtivo(false);
       setEditingAtivoId(null);
@@ -306,7 +343,16 @@ export const InvestimentosPage: React.FC = () => {
                      <div>
                         <div className='flex items-center gap-2'>
                           <span className='font-semibold text-slate-200 text-base'>{ativo.nome}</span>
-                          {ativo.ticker && <span className='bg-slate-800 text-blue-400 font-mono text-[11px]px-2 py-0.5 rounded font-semibold'>{ativo.ticker}</span>}
+                          {ativo.tipo === 'moeda_estrangeira' ? (
+                            <span className='bg-cyan-950/80 border border-cyan-800/50 text-cyan-300 font-mono text-[11px] px-2 py-0.5 rounded font-semibold'>
+                              {ativo.moeda_codigo || 'CÂMBIO'}
+                            </span>
+                          ) : (
+                            ativo.ticker && <span className='bg-slate-800 text-blue-400 font-mono text-[11px] px-2 py-0.5 rounded font-semibold'>{ativo.ticker}</span>
+                          )}
+                          <span className='bg-slate-800/80 text-slate-400 px-2 py-0.5 rounded text-[10px] uppercase font-semibold'>
+                            {LABELS[ativo.tipo] || ativo.tipo}
+                          </span>
                         </div>
                         <div className='flex flex-wrap items-center gap-x-3 gap-y-1 text-xs text-slate-400 mt-1'>
                            {ativo.instituicao && <span className='flex items-center gap-1'><Building2 size={12} /> {ativo.instituicao}</span>}
@@ -317,12 +363,28 @@ export const InvestimentosPage: React.FC = () => {
                     </div>
                     <div className='flex items-center justify-between sm:justify-end gap-4 pt-2 sm:pt-0 border-t sm:border-t-0 border-slate-800/60'>
                       <div className='text-left sm:text-right'>
-                        <div className='text-xs text-slate-500'>Saldo Aplicado</div>
+                        <div className='text-xs text-slate-500'>
+                          {ativo.tipo === 'moeda_estrangeira' ? 'Total em Reais (R$)' : 'Saldo Aplicado'}
+                        </div>
                         <div className='text-base font-bold text-slate-100'><PrivacyValue value={ativo.saldo_aplicado} /></div>
-                        <div className='text-[11px] text-slate-400 mt-0.5'>Qtd: <span className='font-mono text-slate-300'>{ativo.quantidade_total}</span>{ativo.preco_medio > 0 && <> | PM: <span className='font-mono text-slate-300'>R$ {ativo.preco_medio.toFixed(2)}</span></>}</div>
+                        <div className='text-[11px] text-slate-400 mt-0.5'>
+                          {ativo.tipo === 'moeda_estrangeira' ? (
+                            <>
+                              Saldo: <span className='font-mono text-emerald-400 font-semibold'>{Number(ativo.quantidade_total || 0).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })} {ativo.moeda_codigo}</span>
+                              {ativo.preco_medio > 0 && <> | Cotação Média: <span className='font-mono text-slate-300'>R$ {Number(ativo.preco_medio || 0).toFixed(4)}</span></>}
+                            </>
+                          ) : (
+                            <>
+                              Qtd: <span className='font-mono text-slate-300'>{ativo.quantidade_total}</span>
+                              {ativo.preco_medio > 0 && <> | PM: <span className='font-mono text-slate-300'>R$ {Number(ativo.preco_medio || 0).toFixed(2)}</span></>}
+                            </>
+                          )}
+                        </div>
                       </div>
                       <div className='flex items-center gap-1.5'>
-                        <button onClick={() => { setAtivoSelecionado(ativo); setShowModalMov(true); }} className='bg-blue-600/10 hover:bg-blue-600/20 text-blue-400 border border-blue-500/20 px-3 py-1.5 rounded-lg text-xs font-medium transition'>Movimentar</button>
+                        <button onClick={() => { setAtivoSelecionado(ativo); setShowModalMov(true); }} className='bg-blue-600/10 hover:bg-blue-600/20 text-blue-400 border border-blue-500/20 px-3 py-1.5 rounded-lg text-xs font-medium transition'>
+                          {ativo.tipo === 'moeda_estrangeira' ? 'Operar Câmbio' : 'Movimentar'}
+                        </button>
                         <button onClick={() => handleEditarAtivo(ativo)} title="Editar Ativo" className='p-1.5 text-slate-400 hover:text-blue-400 rounded-lg transition'><Edit2 size={16} /></button>
                         <button onClick={() => handleExcluirAtivo(ativo.id, ativo.nome)} title="Excluir Ativo" className='p-1.5 text-slate-500 hover:text-rose-400 rounded-lg transition'><Trash2 size={16} /></button>
                       </div>
@@ -359,39 +421,157 @@ export const InvestimentosPage: React.FC = () => {
                   <option value="moeda_estrangeira">Moeda Estrangeira</option>
                 </select>
               </div>
-              <div>
-                <label className="block text-xs font-semibold text-slate-300 mb-1">Nome do Ativo</label>
-                <input
-                  type="text"
-                  required
-                  placeholder="Ex: Tesouro Selic, PETR4..."
-                  value={formAtivo.nome}
-                  onChange={e => setFormAtivo({ ...formAtivo, nome: e.target.value })}
-                  className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
-                />
-              </div>
-              <div className="grid grid-cols-2 gap-3">
-                <div>
-                  <label className="block text-xs font-semibold text-slate-300 mb-1">Ticker</label>
-                  <input
-                    type="text"
-                    placeholder="Ex: PETR4"
-                    value={formAtivo.ticker}
-                    onChange={e => setFormAtivo({ ...formAtivo, ticker: e.target.value })}
-                    className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
-                  />
-                </div>
-                <div>
-                  <label className="block text-xs font-semibold text-slate-300 mb-1">Instituição</label>
-                  <input
-                    type="text"
-                    placeholder="Ex: XP, Nubank"
-                    value={formAtivo.instituicao}
-                    onChange={e => setFormAtivo({ ...formAtivo, instituicao: e.target.value })}
-                    className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
-                  />
+              {formAtivo.tipo === 'moeda_estrangeira' ? (
+                /* FORMULÁRIO ESPECÍFICO DE MOEDA ESTRANGEIRA / CÂMBIO */
+                <div className="space-y-4 pt-1 border-t border-slate-800">
+                  <div className="grid grid-cols-2 gap-3">
+                    <div>
+                      <label className="block text-xs font-semibold text-slate-300 mb-1">Moeda Estrangeira *</label>
+                      <select
+                        value={formAtivo.moeda_id}
+                        onChange={e => {
+                          const mId = e.target.value;
+                          const mSel = moedas.find(m => m.id === mId);
+                          setFormAtivo({
+                            ...formAtivo,
+                            moeda_id: mId,
+                            nome: formAtivo.nome || (mSel ? `${mSel.nome} em Carteira` : '')
+                          });
+                        }}
+                        className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                      >
+                        {moedas.filter(m => m.codigo !== 'BRL').map(m => (
+                          <option key={m.id} value={m.id}>
+                            {m.codigo} — {m.nome} ({m.simbolo})
+                          </option>
+                        ))}
+                      </select>
+                    </div>
+                    <div>
+                      <label className="block text-xs font-semibold text-slate-300 mb-1">Local de Custódia *</label>
+                      <input
+                        type="text"
+                        required
+                        placeholder="Ex: Nomad, Wise, Cofre Físico..."
+                        value={formAtivo.instituicao}
+                        onChange={e => setFormAtivo({ ...formAtivo, instituicao: e.target.value })}
+                        className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                      />
+                    </div>
+                  </div>
+
+                  <div>
+                    <label className="block text-xs font-semibold text-slate-300 mb-1">Identificação / Nome da Reserva *</label>
+                    <input
+                      type="text"
+                      required
+                      placeholder="Ex: Dólar em Espécie, Conta Nomad, Dólares para Viagem..."
+                      value={formAtivo.nome}
+                      onChange={e => setFormAtivo({ ...formAtivo, nome: e.target.value })}
+                      className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                    />
+                    <div className="flex flex-wrap gap-1.5 mt-2">
+                      {['Dólar em Espécie', 'Conta Nomad', 'Conta Wise', 'C6 Global', 'Euro em Espécie'].map(sug => (
+                        <button
+                          key={sug}
+                          type="button"
+                          onClick={() => setFormAtivo({ ...formAtivo, nome: sug, instituicao: sug.includes('Nomad') ? 'Nomad' : sug.includes('Wise') ? 'Wise' : sug.includes('C6') ? 'Banco C6' : 'Cofre / Espécie' })}
+                          className="px-2 py-0.5 rounded-lg text-[11px] bg-slate-800 hover:bg-slate-700 text-slate-300 border border-slate-700 transition"
+                        >
+                          + {sug}
+                        </button>
+                      ))}
+                    </div>
+                  </div>
+
+                  {!editingAtivoId && (
+                    <div className="p-3 bg-slate-950/70 border border-slate-800 rounded-xl space-y-3">
+                      <div className="flex items-center justify-between">
+                        <label className="text-xs font-semibold text-slate-200 flex items-center gap-2 cursor-pointer">
+                          <input
+                            type="checkbox"
+                            checked={saldoInicialCambio.habilitado}
+                            onChange={e => setSaldoInicialCambio({ ...saldoInicialCambio, habilitado: e.target.checked })}
+                            className="rounded border-slate-700 bg-slate-900 text-blue-600"
+                          />
+                          Já possuo saldo adquirido desta moeda
+                        </label>
+                      </div>
+
+                      {saldoInicialCambio.habilitado && (
+                        <div className="space-y-2 pt-2 border-t border-slate-800/80">
+                          <div className="grid grid-cols-2 gap-2">
+                            <div>
+                              <label className="block text-[11px] text-slate-400 mb-1">Quantidade de Moeda</label>
+                              <input
+                                type="number"
+                                step="0.01"
+                                placeholder="Ex: 500.00"
+                                value={saldoInicialCambio.quantidade}
+                                onChange={e => setSaldoInicialCambio({ ...saldoInicialCambio, quantidade: e.target.value })}
+                                className="w-full bg-slate-900 border border-slate-750 rounded-lg px-2.5 py-1.5 text-xs text-slate-100"
+                              />
+                            </div>
+                            <div>
+                              <label className="block text-[11px] text-slate-400 mb-1">Cotação Média Paga (R$)</label>
+                              <input
+                                type="number"
+                                step="0.0001"
+                                placeholder="Ex: 5.4500"
+                                value={saldoInicialCambio.cotacao}
+                                onChange={e => setSaldoInicialCambio({ ...saldoInicialCambio, cotacao: e.target.value })}
+                                className="w-full bg-slate-900 border border-slate-750 rounded-lg px-2.5 py-1.5 text-xs text-slate-100"
+                              />
+                            </div>
+                          </div>
+                          {Number(saldoInicialCambio.quantidade) > 0 && Number(saldoInicialCambio.cotacao) > 0 && (
+                            <div className="text-xs text-emerald-400 font-medium pt-1">
+                              Total investido em Reais: R$ {(Number(saldoInicialCambio.quantidade) * Number(saldoInicialCambio.cotacao)).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
+                            </div>
+                          )}
+                        </div>
+                      )}
+                    </div>
+                  )}
                 </div>
-              </div>
+              ) : (
+                /* FORMULÁRIO CONVENCIONAL DE AÇÕES / RENDA FIXA / FIIS / CRIPTO */
+                <>
+                  <div>
+                    <label className="block text-xs font-semibold text-slate-300 mb-1">Nome do Ativo *</label>
+                    <input
+                      type="text"
+                      required
+                      placeholder="Ex: Tesouro Selic, PETR4..."
+                      value={formAtivo.nome}
+                      onChange={e => setFormAtivo({ ...formAtivo, nome: e.target.value })}
+                      className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                    />
+                  </div>
+                  <div className="grid grid-cols-2 gap-3">
+                    <div>
+                      <label className="block text-xs font-semibold text-slate-300 mb-1">Ticker / Código</label>
+                      <input
+                        type="text"
+                        placeholder="Ex: PETR4"
+                        value={formAtivo.ticker}
+                        onChange={e => setFormAtivo({ ...formAtivo, ticker: e.target.value })}
+                        className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                      />
+                    </div>
+                    <div>
+                      <label className="block text-xs font-semibold text-slate-300 mb-1">Instituição / Corretora</label>
+                      <input
+                        type="text"
+                        placeholder="Ex: XP, Nubank"
+                        value={formAtivo.instituicao}
+                        onChange={e => setFormAtivo({ ...formAtivo, instituicao: e.target.value })}
+                        className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                      />
+                    </div>
+                  </div>
+                </>
+              )}
               <div className="flex justify-end gap-3 pt-3">
                 <button
                   type="button"
@@ -417,8 +597,10 @@ export const InvestimentosPage: React.FC = () => {
           <div className="bg-slate-900 border border-slate-800 rounded-2xl w-full max-w-md p-6 relative shadow-2xl">
             <div className="flex items-center justify-between mb-4">
               <div>
-                <h3 className="text-lg font-bold text-slate-100">Lançar Movimentação</h3>
-                <p className="text-xs text-slate-400">Ativo: {ativoSelecionado.nome}</p>
+                <h3 className="text-lg font-bold text-slate-100">
+                  {ativoSelecionado.tipo === 'moeda_estrangeira' ? 'Operação de Câmbio' : 'Lançar Movimentação'}
+                </h3>
+                <p className="text-xs text-slate-400">Ativo: {ativoSelecionado.nome} {ativoSelecionado.moeda_codigo ? `(${ativoSelecionado.moeda_codigo})` : ''}</p>
               </div>
               <button onClick={() => setShowModalMov(false)} className="text-slate-400 hover:text-slate-200">
                 <X size={20} />
@@ -426,46 +608,151 @@ export const InvestimentosPage: React.FC = () => {
             </div>
             <form onSubmit={handleSalvarMovimentacao} className="space-y-4">
               <div>
-                <label className="block text-xs font-semibold text-slate-300 mb-1">Tipo</label>
+                <label className="block text-xs font-semibold text-slate-300 mb-1">
+                  {ativoSelecionado.tipo === 'moeda_estrangeira' ? 'Tipo de Operação' : 'Tipo'}
+                </label>
                 <div className="grid grid-cols-3 gap-2">
-                  {(['aporte', 'resgate', 'rendimento'] as const).map(tipo => (
-                    <button
-                      type="button"
-                      key={tipo}
-                      onClick={() => setFormMov({ ...formMov, tipo })}
-                      className={`py-2 rounded-xl text-xs font-medium capitalize border transition ${
-                        formMov.tipo === tipo
-                          ? 'bg-blue-600 text-white border-blue-500'
-                          : 'bg-slate-950 text-slate-400 border-slate-800'
-                      }`}
-                    >
-                      {tipo}
-                    </button>
-                  ))}
+                  {(['aporte', 'resgate', 'rendimento'] as const).map(tipo => {
+                    const label = ativoSelecionado.tipo === 'moeda_estrangeira'
+                      ? tipo === 'aporte'
+                        ? 'Comprar'
+                        : tipo === 'resgate'
+                        ? 'Vender / Usar'
+                        : 'Rendimento'
+                      : tipo;
+                    return (
+                      <button
+                        type="button"
+                        key={tipo}
+                        onClick={() => setFormMov({ ...formMov, tipo })}
+                        className={`py-2 rounded-xl text-xs font-medium capitalize border transition ${
+                          formMov.tipo === tipo
+                            ? 'bg-blue-600 text-white border-blue-500'
+                            : 'bg-slate-950 text-slate-400 border-slate-800'
+                        }`}
+                      >
+                        {label}
+                      </button>
+                    );
+                  })}
                 </div>
               </div>
+
+              {ativoSelecionado.tipo === 'moeda_estrangeira' ? (
+                /* INTERFACE DE CÂMBIO */
+                <div className="space-y-3">
+                  <div className="grid grid-cols-2 gap-3">
+                    <div>
+                      <label className="block text-xs font-semibold text-slate-300 mb-1">
+                        Qtd ({ativoSelecionado.moeda_codigo || 'Moeda'}) *
+                      </label>
+                      <input
+                        type="number"
+                        step="0.01"
+                        required
+                        placeholder="Ex: 100.00"
+                        value={formMov.quantidade}
+                        onChange={e => {
+                          const qtd = e.target.value;
+                          const cot = formMov.cotacao_praticada;
+                          const novoValor = (parseFloat(qtd) > 0 && parseFloat(cot) > 0)
+                            ? (parseFloat(qtd) * parseFloat(cot)).toFixed(2)
+                            : formMov.valor;
+                          setFormMov({ ...formMov, quantidade: qtd, valor: novoValor });
+                        }}
+                        className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                      />
+                    </div>
+                    <div>
+                      <label className="block text-xs font-semibold text-slate-300 mb-1">
+                        Cotação Paga (R$) *
+                      </label>
+                      <input
+                        type="number"
+                        step="0.0001"
+                        required
+                        placeholder="Ex: 5.4500"
+                        value={formMov.cotacao_praticada}
+                        onChange={e => {
+                          const cot = e.target.value;
+                          const qtd = formMov.quantidade;
+                          const novoValor = (parseFloat(qtd) > 0 && parseFloat(cot) > 0)
+                            ? (parseFloat(qtd) * parseFloat(cot)).toFixed(2)
+                            : formMov.valor;
+                          setFormMov({ ...formMov, cotacao_praticada: cot, valor: novoValor });
+                        }}
+                        className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                      />
+                    </div>
+                  </div>
+                  <div>
+                    <label className="block text-xs font-semibold text-slate-300 mb-1">Total em Reais (R$) *</label>
+                    <input
+                      type="number"
+                      step="0.01"
+                      required
+                      placeholder="0,00"
+                      value={formMov.valor}
+                      onChange={e => {
+                        const val = e.target.value;
+                        const qtd = formMov.quantidade;
+                        const novaCot = (parseFloat(val) > 0 && parseFloat(qtd) > 0)
+                          ? (parseFloat(val) / parseFloat(qtd)).toFixed(4)
+                          : formMov.cotacao_praticada;
+                        setFormMov({ ...formMov, valor: val, cotacao_praticada: novaCot });
+                      }}
+                      className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                    />
+                  </div>
+                </div>
+              ) : (
+                /* INTERFACE PADRÃO */
+                <div className="grid grid-cols-2 gap-3">
+                  <div>
+                    <label className="block text-xs font-semibold text-slate-300 mb-1">Valor (R$)</label>
+                    <input
+                      type="number"
+                      step="0.01"
+                      required
+                      placeholder="0,00"
+                      value={formMov.valor}
+                      onChange={e => setFormMov({ ...formMov, valor: e.target.value })}
+                      className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                    />
+                  </div>
+                  <div>
+                    <label className="block text-xs font-semibold text-slate-300 mb-1">Quantidade</label>
+                    <input
+                      type="number"
+                      step="0.00000001"
+                      required
+                      placeholder="1"
+                      value={formMov.quantidade}
+                      onChange={e => setFormMov({ ...formMov, quantidade: e.target.value })}
+                      className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
+                    />
+                  </div>
+                </div>
+              )}
+
               <div className="grid grid-cols-2 gap-3">
                 <div>
-                  <label className="block text-xs font-semibold text-slate-300 mb-1">Valor (R$)</label>
+                  <label className="block text-xs font-semibold text-slate-300 mb-1">Data</label>
                   <input
-                    type="number"
-                    step="0.01"
+                    type="date"
                     required
-                    placeholder="0,00"
-                    value={formMov.valor}
-                    onChange={e => setFormMov({ ...formMov, valor: e.target.value })}
+                    value={formMov.data}
+                    onChange={e => setFormMov({ ...formMov, data: e.target.value })}
                     className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
                   />
                 </div>
                 <div>
-                  <label className="block text-xs font-semibold text-slate-300 mb-1">Quantidade</label>
+                  <label className="block text-xs font-semibold text-slate-300 mb-1">Observação</label>
                   <input
-                    type="number"
-                    step="0.00000001"
-                    required
-                    placeholder="1"
-                    value={formMov.quantidade}
-                    onChange={e => setFormMov({ ...formMov, quantidade: e.target.value })}
+                    type="text"
+                    placeholder="Opcional..."
+                    value={formMov.observacao}
+                    onChange={e => setFormMov({ ...formMov, observacao: e.target.value })}
                     className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-sm text-slate-200 focus:outline-none"
                   />
                 </div>
diff --git a/frontend/src/pages/PixPage.tsx b/frontend/src/pages/PixPage.tsx
index b753d8c..2fab141 100644
--- a/frontend/src/pages/PixPage.tsx
+++ b/frontend/src/pages/PixPage.tsx
@@ -1,5 +1,5 @@
 import React, { useState, useEffect } from 'react';
-import { QrCode, Copy, Check, Plus, Trash2, CheckCircle, Key, RefreshCw } from 'lucide-react';
+import { QrCode, Copy, Check, Plus, Trash2, CheckCircle, Key, RefreshCw, Landmark } from 'lucide-react';
 import { api } from '../services/api';
 import { ChavePix, CobrancaPix, Divida, Conta } from '../types';
 import { PrivacyValue } from '../components/common/PrivacyValue';
@@ -23,7 +23,8 @@ export const PixPage: React.FC = () => {
     valor_chave: '',
     nome_recebedor: '',
     cidade_recebedor: 'SAO PAULO',
-    apelido: ''
+    apelido: '',
+    conta_id: ''
   });
 
   // Form Cobrança
@@ -68,7 +69,8 @@ export const PixPage: React.FC = () => {
         valor_chave: '',
         nome_recebedor: '',
         cidade_recebedor: 'SAO PAULO',
-        apelido: ''
+        apelido: '',
+        conta_id: ''
       });
       carregarDados();
     } catch (err: any) {
@@ -204,11 +206,18 @@ export const PixPage: React.FC = () => {
                     {ch.nome_recebedor} • {ch.cidade_recebedor}
                   </div>
                 </div>
-                {ch.apelido && (
-                  <span className="text-xs text-indigo-500 font-medium mt-2 block">
-                    {ch.apelido}
-                  </span>
-                )}
+                <div className="mt-2 space-y-1">
+                  {ch.conta_nome && (
+                    <span className="inline-flex items-center gap-1 text-[11px] font-semibold text-emerald-600 dark:text-emerald-400 bg-emerald-50 dark:bg-emerald-950/40 px-2 py-0.5 rounded">
+                      <Landmark className="w-3 h-3" /> Conta: {ch.conta_nome}
+                    </span>
+                  )}
+                  {ch.apelido && (
+                    <span className="text-xs text-indigo-500 font-medium block">
+                      {ch.apelido}
+                    </span>
+                  )}
+                </div>
               </div>
             ))}
           </div>
@@ -421,11 +430,30 @@ export const PixPage: React.FC = () => {
                     type="text"
                     value={formChave.apelido}
                     onChange={e => setFormChave({ ...formChave, apelido: e.target.value })}
-                    placeholder="Ex: Nubank"
+                    placeholder="Ex: Nubank Principal"
                     className="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white text-sm"
                   />
                 </div>
               </div>
+
+              <div>
+                <label className="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">
+                  Conta Bancária Vinculada (onde o dinheiro cai)
+                </label>
+                <select
+                  value={formChave.conta_id}
+                  onChange={e => setFormChave({ ...formChave, conta_id: e.target.value })}
+                  className="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white text-sm"
+                >
+                  <option value="">Selecione uma conta...</option>
+                  {contas.map(c => (
+                    <option key={c.id} value={c.id}>{c.apelido} ({c.instituicao_nome || 'Banco'})</option>
+                  ))}
+                </select>
+                <span className="text-[11px] text-gray-400 mt-0.5 block">
+                  Ao confirmar cobranças desta chave, os valores serão creditados nesta conta.
+                </span>
+              </div>
               <div className="flex justify-end gap-2 pt-3">
                 <button
                   type="button"
diff --git a/frontend/src/pages/SimuladorGastosPage.tsx b/frontend/src/pages/SimuladorGastosPage.tsx
index e597c64..a0cd295 100644
--- a/frontend/src/pages/SimuladorGastosPage.tsx
+++ b/frontend/src/pages/SimuladorGastosPage.tsx
@@ -31,7 +31,7 @@ export const SimuladorGastosPage: React.FC = () => {
         ]);
         setContas(cRes);
         setCartoes(crtRes);
-        if (cRes.length > 0) setForm(prev => ({ ...prev, conta_id: cRes[0].id }));
+        setForm(prev => ({ ...prev, conta_id: 'unificado' }));
         if (crtRes.length > 0) setForm(prev => ({ ...prev, cartao_id: crtRes[0].id }));
       } catch (err) {
         console.error('Erro ao carregar dados:', err);
@@ -178,12 +178,15 @@ export const SimuladorGastosPage: React.FC = () => {
 
             {form.forma_pagamento === 'a_vista' ? (
               <div>
-                <label className="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">Conta Bancária</label>
+                <label className="block text-xs font-medium text-gray-700 dark:text-gray-300 mb-1">Conta Bancária / Origem</label>
                 <select
-                  value={form.conta_id}
+                  value={form.conta_id || 'unificado'}
                   onChange={e => setForm({ ...form, conta_id: e.target.value })}
                   className="w-full px-3 py-2 border rounded-lg dark:bg-gray-700 dark:border-gray-600 dark:text-white text-sm"
                 >
+                  <option value="unificado">
+                    🌐 Saldo Unificado (Todas as Contas — R$ {contas.reduce((acc, c) => acc + Number(c.saldo_atual || 0), 0).toFixed(2)})
+                  </option>
                   {contas.map(c => (
                     <option key={c.id} value={c.id}>{c.apelido} (Saldo: R$ {Number(c.saldo_atual || 0).toFixed(2)})</option>
                   ))}
diff --git a/frontend/src/pages/SocialPage.tsx b/frontend/src/pages/SocialPage.tsx
index 743d342..83d81c0 100644
--- a/frontend/src/pages/SocialPage.tsx
+++ b/frontend/src/pages/SocialPage.tsx
@@ -1172,7 +1172,7 @@ export const SocialPage: React.FC = () => {
                   >
                     {chavesPix.map(ch => (
                       <option key={ch.id} value={ch.id}>
-                        {ch.apelido ? `[${ch.apelido}] ` : ''}{ch.tipo.toUpperCase()}: {ch.valor_chave} ({ch.nome_recebedor})
+                        {ch.apelido ? `[${ch.apelido}] ` : ''}{ch.tipo.toUpperCase()}: {ch.valor_chave} {ch.conta_nome ? `— Destino: ${ch.conta_nome}` : `(${ch.nome_recebedor})`}
                       </option>
                     ))}
                   </select>
diff --git a/frontend/src/types/index.ts b/frontend/src/types/index.ts
index 78f4105..c3cf7d2 100644
--- a/frontend/src/types/index.ts
+++ b/frontend/src/types/index.ts
@@ -377,6 +377,8 @@ export interface ChavePix {
   nome_recebedor: string;
   cidade_recebedor: string;
   apelido?: string;
+  conta_id?: string;
+  conta_nome?: string;
   ativo: boolean;
   criado_em: string;
 }
-- 
2.55.0.windows.5

